# [0077. Combinations](https://leetcode.cn/problems/combinations/)

## Description

给定两个整数 `n` 和 `k`，返回范围 `[1, n]` 中所有可能的 `k` 个数的组合。

你可以按 **任何顺序** 返回答案。

 

**示例 1：**

```
输入：n = 4, k = 2
输出：
[
  [2,4],
  [3,4],
  [2,3],
  [1,2],
  [1,3],
  [1,4],
]
```

**示例 2：**

```
输入：n = 1, k = 1
输出：[[1]]
```

 

**提示：**

- `1 <= n <= 20`
- `1 <= k <= n`



## Solutions

题意需要穷举在 1~N 之间的 k个数字的组合关系 这就需要用到 回溯算法
已经限制 n <= 20 无需考虑 timeout 的情况

```java
import java.util.ArrayList;

public static List<int[]> backtracking(int n, int k) {
    List<int[]> res = new ArrayList<>();
   
    
    return res;
}

public static void getCombine(int n, int k){
    for(int i = 0; i <= n; i++){
        int[] arr = new int[k];
        for(int j = i; j <= n; j++){

        }


    }
}
```

