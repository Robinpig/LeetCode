# 回文链表

## Description

编写一个函数，检查链表是否为回文



## Solution

判断一个链表是否回文 对于双向链表来说是非常简单的 只需要从首位往中间遍历即可





对于单向链表 

方法一  反转整个链表 再对两个链表进行遍历 遍历可以只遍历前半部分

```java
class Solution{
  boolean isPalindrome(ListNode list){
    
  }
}
```



方法二 快慢指针进行遍历 同时将前半部分借助栈存储 之后就是后半部分与栈中数据的比较



```java
class Solution{
  boolean isPalindrome(ListNode list){
    
  }
}
```







方法三 使用递归 此方法需要先遍历一次获取到 length 递归比较子链表(**传入head.next 和 length - 2**), 直到中间节点为终止条件
递归比较是从中间往两边进行比较



```java
class Solution{
  boolean isPalindrome(ListNode list){
    
  }
  
  res recursion(ListNode head, length){
    if(head == null || length <= 0){
      
    } else if(length == 1){
      
    }
    
   res = recursion(head.next, length - 2)

	 if (!res.result || res.node == null) {
	 		return res;
	 }

	 res.result = (head.data == res.node.data);

	 res.node = res.node.next;
	
	 return res;
  }
}
```





