# 移除重复节点

## Descritption

编写代码，移除未排序链表中的重复节点。

进阶：如果不得使用临时缓冲区，该怎么解决？





## Solutions

首先需要知道哪些节点是重复的

遍历链表 使用一个map记录节点数值 如果重复出现则 使用后继节点replace

时间复杂度为 $O(n)$ 空间复杂度为  $O(n)$ 



```java
class Solution{
  void removeDuplicateNode(ListNode list){
    
  }
}
```





不使用额外空间 则需要通过两个指针 一个 cur 指向当前遍历数值, 另一个用于遍历后续节点 判断是否有重复节点

时间复杂度为 $O(n^2)$ 空间复杂度为  $O(1)$ 



```java
class Solution{
  void removeDuplicateNode(ListNode list){
    
  }
}
```

