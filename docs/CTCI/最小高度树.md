# 最小高度树

## Description

给定一个有序整数数组，元素各不相同且按升序排列，编写一个算法，创建一棵高度最小的二叉搜索树

## Solutions

高度最小 意味着 rootNode 需要为数组的中间值

利用对数组的 binary search 获取到中间节点 左区间为左子树 右区间为右子树 
依次递归 返回每一层的 rootNode 递归终止条件是区间内 left >= right


```java
class Solution {
    void dfs(int[] nums){
        
    }
}
```

